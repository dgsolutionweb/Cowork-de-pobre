---
story_id: 1.4
title: "Persistir Memórias Automáticas"
status: Draft
epic_id: 1
epic_title: "Etapa 1: Projects + instructions + memory + RAG"
created_at: 2026-04-20
updated_at: 2026-04-20
---

## Story Statement

Como usuário, quero que o assistente persista automaticamente fatos, decisões e preferências durante nossas conversas, para não precisar repetir contexto importante em sessões futuras.

## Acceptance Criteria

1. **Extração automática** - Sistema identifica fatos/decisões/preferências nas mensagens do assistente
2. **Categorização** - Memórias salvas com categoria: `fact`, `decision`, `preference`, `summary`
3. **Confirmação opcional** - Usuário pode revisar antes de salvar (configurável)
4. **Recall automático** - Memórias relevantes injetadas no contexto inicial do chat
5. **UI de gerenciamento** - Usuário pode ver, editar e excluir memórias salvas
6. **Por projeto** - Memórias vinculadas ao `activeProjectId`

## Scope

**IN:**
- Prompt de sistema para extração de memórias (LLM-based)
- `ProjectMemoriesService` com métodos `extractAndSave`, `getRelevant`
- IPC handlers: `projects:extract-memory`, `projects:get-relevant-memories`
- UI de lista de memórias em `/projects` ou página dedicada
- Toggle "Salvamemória automática" em Configurações

**OUT:**
- Memórias cruzadas entre projetos
- Vetorização de memórias (fase 2)
- Exportação/importação de memórias

## Dependencies

- ✅ `project_memories` table
- ✅ `ProjectMemoriesRepository` (upsert, list, delete)
- ✅ `activeProjectId` nas preferências
- ⏳ Story 1.3 (UI de Pesquisa) - reutilizar padrões

## Dev Notes

### Backend Pronto

**Repository:** `ProjectMemoriesRepository` (`electron/repositories/projectMemoriesRepository.ts`)
- `upsert(memory)` - Insert ou update por key
- `listByProject(projectId)` - Lista memórias do projeto
- `delete(projectId, key)` - Remove memória
- `clearProject(projectId)` - Limpa todas

**Schema:** `database.ts` linhas 163-173
```sql
CREATE TABLE IF NOT EXISTS project_memories (
  id TEXT PRIMARY KEY,
  project_id TEXT NOT NULL,
  key TEXT NOT NULL,
  value TEXT NOT NULL,
  category TEXT NOT NULL, -- 'fact', 'decision', 'preference', 'summary'
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE,
  UNIQUE(project_id, key)
);
```

### O Que Implementar

**1. Service: `ProjectMemoriesService`**
```typescript
class ProjectMemoriesService {
  // Extrai memórias de uma conversa usando LLM
  async extractAndSave(
    projectId: string,
    conversation: Message[]
  ): Promise<Memory[]>;

  // Busca memórias relevantes para um contexto
  async getRelevant(projectId: string, context: string): Promise<Memory[]>;

  // Injeta memórias no prompt do sistema
  buildMemoryContext(memories: Memory[]): string;
}
```

**2. Prompt de Extração (Gemini)**
```
Extraia do diálogo abaixo:
- FACTS: Fatos objetivos sobre o projeto/código
- DECISIONS: Decisões tomadas (ex: "usar X em vez de Y")
- PREFERENCES: Preferências do usuário (ex: "prefere testes unitários")
- SUMMARIES: Resumos de discussões longas

Retorne JSON: { key, value, category }
```

**3. IPC Handlers Novos**
```typescript
ipcMain.handle("projects:extract-memory", async (_event, projectId: string, conversationId: string) =>
  memoriesService.extractAndSave(projectId, conversationId)
);

ipcMain.handle("projects:get-relevant-memories", async (_event, projectId: string, query: string) =>
  memoriesService.getRelevant(projectId, query)
);
```

**4. Integração no Chat**
- Após resposta do assistente, extrair memórias em background
- Injetar memórias relevantes no prompt inicial

**5. UI**
- Lista de memórias em `Settings` ou página `Projects`
- Toggle "Auto-save memories"

## Tasks

### Task 1: Criar serviço de memórias (AC: 1, 2, 4)
- [ ] Criar `electron/services/projectMemoriesService.ts`
- [ ] Implementar `extractAndSave()` com prompt Gemini
- [ ] Implementar `getRelevant()` com busca por categoria
- [ ] Implementar `buildMemoryContext()`

### Task 2: Adicionar IPC handlers (AC: 1, 2, 4)
- [ ] `projects:extract-memory`
- [ ] `projects:get-relevant-memories`
- [ ] `projects:delete-memory`

### Task 3: Integrar no chat (AC: 1, 4)
- [ ] Extrair memórias após resposta do assistente (background)
- [ ] Injetar memórias no contexto inicial do Gemini
- [ ] Toggle "auto-save" em preferências

### Task 4: UI de gerenciamento (AC: 3, 5)
- [ ] Seção "Memórias do Projeto" em `/projects` ou `/settings`
- [ ] Lista com filtro por categoria
- [ ] Botões editar/excluir
- [ ] Toggle "Salvamemória automática"

### Task 5: Testes (AC: 1, 2, 3, 4, 5, 6)
- [ ] Testar extração de memórias
- [ ] Testar recall de memórias
- [ ] Testar UI de gerenciamento

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

## Change Log

| Data | Agente | Mudança |
|------|--------|---------|
| 2026-04-20 | @sm | Story criada - Persistência automática de memórias |
